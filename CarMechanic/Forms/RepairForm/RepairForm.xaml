<Window x:Class="CarMechanic.RepairForm.RepairForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Repair Form" Height="400" Width="400">
    <Grid>
        <StackPanel Margin="10">
            <TextBlock Text="Description" />
            <TextBox x:Name="DescriptionTextBox" Margin="0,0,0,10" />

            <TextBlock Text="Date" />
            <DatePicker x:Name="RepairDatePicker" Margin="0,0,0,10" />

            <TextBlock Text="Parts" />
            <ListBox x:Name="PartsListBox" ItemsSource="{Binding Parts}" DisplayMemberPath="Name" Margin="0,0,0,10" />

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Save" Width="75" Margin="0,0,5,0" Click="SaveButton_Click" />
                <Button Content="Cancel" Width="75" Click="CancelButton_Click" />
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
